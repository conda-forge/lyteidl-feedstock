{% set name = "lyteidl" %}
{% set version = "0.3.0" %}
{% set python_max = "3.11" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/lyteidl-{{ version }}.tar.gz
  sha256: ecfa4842c7e85a7bab8acb05c6a859d447e4df789a16571f37b7e2df7a67efb3

build:
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  noarch: python
  number: 0
  script_env:
    - VERSION={{ version }}

requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools
  run:
    - python >={{ python_min }},<={{ python_max }}
    - googleapis-common-protos
    - protoc-gen-swagger
    - protobuf >=3.5.0,<4.0.0

test:
  imports:
    - flyteidl
    - validate
  commands:
    - pip check
  requires:
    - python {{ python_min }}
    - pip

about:
  home: https://www.github.com/flyteorg/flyteidl
  summary: IDL for Flyte Platform
  license: MIT AND Apache-2.0
  license_file:
    - LICENSE
    - NOTICE

extra:
  recipe-maintainers:
    - nh13
